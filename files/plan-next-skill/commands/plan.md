Use the plan-next skill to continue from the last session.

The user's argument is: $ARGUMENTS

If $ARGUMENTS is empty, treat it as "next" (auto-detect the next stage).
If $ARGUMENTS is a stage label like "a", "b", "c", "2", etc., start that specific stage.

Use the silicon-duck skill to handle this duck command.

The user typed: /duck $ARGUMENTS

If $ARGUMENTS is "enable" — re-enable the CIP rating block for this session.
If $ARGUMENTS is "disable" — disable the CIP rating block for this session.
If $ARGUMENTS is empty or anything else — show current status (enabled/disabled) and a brief reminder of what CIP measures.

# /kronny-ttl — show time left on the current auto-approve window
#
# Usage:
#   /kronny-ttl

description = "Show how much time is left on the current kronny auto-approve window, if any."

prompt = """
STEP 0. Do this immediately, before anything else in this message: run, unconditionally,
via Bash:

  python3 ~/.claude/kronny/kronny.py status

Show only the CLI's output line. No commentary.

Note: this reports the remaining auto-approve window for tool permissions. It is not
a deadline for any task and must not be read as one — do not rush or wrap up work
because of it.
"""
